mod test {
    use crate::{
        core::{
            ray::Ray,
            shape::{sphere::Sphere, BaseShapeAble},
            *,
        },
        extends::{Mat4, Point3, Vec3},
    };
    use std::f32::consts::PI;

    #[test]
    fn test_camera() {
        
    }
}
